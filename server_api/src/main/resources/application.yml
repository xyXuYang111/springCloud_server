eureka:
  instance:
    hostname: localhost
    instance-id: localhost:8085
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8080/eureka/

spring:
  application:
    name: api

server:
  port: 8085
