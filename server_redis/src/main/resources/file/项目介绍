1，这个是文件服务项目，里面集成了redis，mysql，mybatis，熔断，fegihn等相关配置
       1，mysql，mybatis，redis，ribbon，feign，Hystrix
       2，此服务需要实现效果是：看到我所有的的文件，对我所有文件进行统一管理
           需要集成一个别人已经写好的项目做简单的会用

2，今日开发：
       实现内容写入到文本中，同时存到数据库中
          完成进度：相关数据进行了定义，具体完成需要明天

        后面开发：做email服务，通过调用用户服务查询需要发送任务
        开发用户信息服务

3，文件流的笔记
       文件流的写入情况和自己定义的byte有关，如果这个定义的较低，那就会进行分割操作。

       文件流的操作：将文件流必须指定写入到某个指定位置，然后根据写入的这个流进行筛选