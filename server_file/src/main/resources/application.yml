eureka:
  instance:
    hostname: localhost
    instance-id: localhost:8081
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8080/eureka/

server:
  port: 8082

spring:
  application:
    name: server-file
