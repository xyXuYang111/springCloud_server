eureka:
  instance:
    hostname: localhost
    instance-id: localhost:8083
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8080/eureka/

spring:
  application:
    name: email

server:
  port: 8083
